'use client';
import { AlertTriangle, Bell, Search } from 'lucide-react';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import type { CrmSettings, Deal, DealStatus, Reminder } from '@/types/crm';
import { formatDate, formatDateTime, formatMoney } from '@/lib/documents/generateDocumentText';

export function WorkspaceHeader({ title, totalDeals, totalValue, settings, upcomingReminders, overdueDeals, statusTitles, searchQuery, setSearchQuery, filteredClientsCount, filteredDealsCount, hasSearchQuery, onCompleteReminder }: { title: string; totalDeals: number; totalValue: number; settings: CrmSettings; upcomingReminders: Reminder[]; overdueDeals: Deal[]; statusTitles: Record<DealStatus, string>; searchQuery: string; setSearchQuery: (value: string) => void; filteredClientsCount: number; filteredDealsCount: number; hasSearchQuery: boolean; onCompleteReminder: (id: string) => void }) {
  return <><header className="border-b border-slate-200 bg-white px-5 py-5 sm:px-8"><div className="flex flex-col gap-5 xl:flex-row xl:items-center xl:justify-between"><div><p className="mb-2 text-sm font-semibold uppercase tracking-[0.2em] text-slate-500">Рабочая область</p><h1 className="text-3xl font-bold tracking-tight text-slate-950 sm:text-4xl">{title}</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">Переключайтесь между разделами CRM: сводкой, клиентами, сделками, чертежами, файлами и настройками.</p></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-[repeat(2,minmax(120px,auto))_minmax(320px,420px)]"><div className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3"><p className="text-xs font-semibold uppercase text-slate-500">Сделок</p><p className="text-2xl font-bold text-slate-950">{totalDeals}</p></div><div className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3"><p className="text-xs font-semibold uppercase text-slate-500">Портфель</p><p className="text-2xl font-bold text-slate-950">{formatMoney(totalValue, settings.money)}</p></div><section className="rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 sm:col-span-2 xl:col-span-1" aria-label="Уведомления"><div className="mb-3 flex items-center justify-between gap-3"><div className="flex items-center gap-2"><Bell className="h-4 w-4 text-amber-700" /><h2 className="text-sm font-bold text-slate-950">Уведомления</h2></div><Badge className="border-amber-200 bg-white text-amber-700">{upcomingReminders.length + overdueDeals.length}</Badge></div><div className="space-y-3 text-xs"><div><p className="mb-2 font-semibold uppercase tracking-[0.14em] text-amber-700">Сегодня / завтра</p>{upcomingReminders.length > 0 ? <ul className="space-y-2">{upcomingReminders.map((reminder) => <li key={reminder.id} className="rounded-xl bg-white p-2 text-slate-700"><div className="flex items-start justify-between gap-2"><div><p className="font-semibold text-slate-950">{reminder.title}</p><p className="mt-1 text-slate-500">{formatDateTime(reminder.dueAt)} · {reminder.managerId}</p></div><Button type="button" size="sm" variant="outline" onClick={() => onCompleteReminder(reminder.id)}>Готово</Button></div></li>)}</ul> : <p className="rounded-xl bg-white p-2 text-slate-500">Нет ближайших напоминаний.</p>}</div><div><p className="mb-2 flex items-center gap-1 font-semibold uppercase tracking-[0.14em] text-red-700"><AlertTriangle className="h-3.5 w-3.5" />Просрочено</p>{overdueDeals.length > 0 ? <ul className="space-y-2">{overdueDeals.map((deal) => <li key={deal.id} className="rounded-xl border border-red-100 bg-white p-2 text-slate-700"><p className="font-semibold text-slate-950">{deal.title}</p><p className="mt-1">Клиент: {deal.client}</p><p className="mt-1">Срок: {formatDate(deal.dueDate)}</p><p className="mt-1">Статус: {statusTitles[deal.status] ?? deal.status}</p></li>)}</ul> : <p className="rounded-xl bg-white p-2 text-slate-500">Нет просроченных сделок.</p>}</div></div></section></div></div></header><section className="border-b border-slate-200 bg-white px-5 py-4 sm:px-8"><label htmlFor="workspace-search" className="mb-2 block text-sm font-semibold text-slate-700">Поиск по клиентам и сделкам</label><div className="relative max-w-3xl"><Search className="pointer-events-none absolute left-4 top-1/2 h-5 w-5 -translate-y-1/2 text-slate-400" /><Input id="workspace-search" type="search" value={searchQuery} onChange={(event) => setSearchQuery(event.target.value)} placeholder="Введите имя, компанию, телефон, email, номер сделки, название или заметку" className="pl-12" /></div>{hasSearchQuery ? <p className="mt-3 text-sm text-slate-500">Найдено клиентов: {filteredClientsCount}, сделок: {filteredDealsCount}.</p> : null}</section></>;
}
