'use client';
import { Plus } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import type { CostCategoryConfig, CrmSettings, DealCostCategory, DealStatus, DealStatusConfig, DocumentKind, DocumentTemplateConfig, TeamMember } from '@/types/crm';
export function SettingsPanel({ settings, message, updateTeamMember, addTeamMember, updateDealStatusConfig, updateCostCategoryConfig, updateDocumentTemplateConfig, updateCompanyField, updateMoneyField }: { settings: CrmSettings; message: string | null; updateTeamMember: (id: string, patch: Partial<TeamMember>) => void; addTeamMember: () => void; updateDealStatusConfig: (id: DealStatus, patch: Partial<DealStatusConfig>) => void; updateCostCategoryConfig: (id: DealCostCategory, patch: Partial<CostCategoryConfig>) => void; updateDocumentTemplateConfig: (id: DocumentKind, patch: Partial<DocumentTemplateConfig>) => void; updateCompanyField: (field: keyof CrmSettings['company'], value: string) => void; updateMoneyField: (field: keyof CrmSettings['money'], value: string | boolean) => void }) {
  return <section className="flex-1 space-y-5 px-5 py-6 sm:px-8">{message ? <p className="rounded-2xl border border-emerald-200 bg-emerald-50 p-3 text-sm font-semibold text-emerald-700">{message}</p> : null}<div className="grid gap-5 xl:grid-cols-2"><Card><CardHeader><CardTitle>Менеджеры и роли</CardTitle><CardDescription>Список ответственных сотрудников для сделок и напоминаний.</CardDescription></CardHeader><CardContent className="space-y-3">{settings.teamMembers.map((member) => <div key={member.id} className="grid gap-2 rounded-2xl border border-slate-200 p-3 md:grid-cols-2"><Input value={member.name} onChange={(event) => updateTeamMember(member.id, { name: event.target.value })} placeholder="Имя" /><Input value={member.role} onChange={(event) => updateTeamMember(member.id, { role: event.target.value })} placeholder="Роль" /><Input value={member.email} onChange={(event) => updateTeamMember(member.id, { email: event.target.value })} placeholder="Email" /><Input value={member.phone} onChange={(event) => updateTeamMember(member.id, { phone: event.target.value })} placeholder="Телефон" /><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={member.isActive} onChange={(event) => updateTeamMember(member.id, { isActive: event.target.checked })} /> Активен</label></div>)}<Button type="button" variant="outline" onClick={addTeamMember}><Plus className="h-4 w-4" /> Добавить менеджера</Button></CardContent></Card><Card><CardHeader><CardTitle>Статусы сделок</CardTitle><CardDescription>Названия, порядок и визуальный стиль канбан-колонок.</CardDescription></CardHeader><CardContent className="space-y-3">{settings.dealStatuses.map((status) => <div key={status.id} className="grid gap-2 rounded-2xl border border-slate-200 p-3 md:grid-cols-[1fr_120px]"><Input value={status.title} onChange={(event) => updateDealStatusConfig(status.id, { title: event.target.value })} /><Input type="number" value={status.sortOrder} onChange={(event) => updateDealStatusConfig(status.id, { sortOrder: Number(event.target.value) || 0 })} /><Input value={status.colorClassName} onChange={(event) => updateDealStatusConfig(status.id, { colorClassName: event.target.value })} className="md:col-span-2" /><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={status.isActive} onChange={(event) => updateDealStatusConfig(status.id, { isActive: event.target.checked })} /> Активен</label></div>)}</CardContent></Card><Card><CardHeader><CardTitle>Категории расходов</CardTitle><CardDescription>Категории сметы, доступные в форме сделки.</CardDescription></CardHeader><CardContent className="space-y-3">{settings.costCategories.map((category) => <div key={category.id} className="grid gap-2 rounded-2xl border border-slate-200 p-3 md:grid-cols-[1fr_120px]"><Input value={category.title} onChange={(event) => updateCostCategoryConfig(category.id, { title: event.target.value })} /><Input type="number" value={category.sortOrder} onChange={(event) => updateCostCategoryConfig(category.id, { sortOrder: Number(event.target.value) || 0 })} /><Input value={category.description} onChange={(event) => updateCostCategoryConfig(category.id, { description: event.target.value })} className="md:col-span-2" /><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={category.isActive} onChange={(event) => updateCostCategoryConfig(category.id, { isActive: event.target.checked })} /> Активна</label></div>)}</CardContent></Card><Card><CardHeader><CardTitle>Деньги и округление</CardTitle><CardDescription>Формат сумм в карточках, сметах и документах.</CardDescription></CardHeader><CardContent className="grid gap-3 md:grid-cols-2"><Input type="number" value={settings.money.minimumFractionDigits} onChange={(event) => updateMoneyField('minimumFractionDigits', event.target.value)} placeholder="Мин. знаков" /><Input type="number" value={settings.money.maximumFractionDigits} onChange={(event) => updateMoneyField('maximumFractionDigits', event.target.value)} placeholder="Макс. знаков" /><Input type="number" value={settings.money.roundingIncrement} onChange={(event) => updateMoneyField('roundingIncrement', event.target.value)} placeholder="Шаг округления" /><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={settings.money.applyRoundingToDocuments} onChange={(event) => updateMoneyField('applyRoundingToDocuments', event.target.checked)} /> Округлять в документах</label></CardContent></Card><Card><CardHeader><CardTitle>Реквизиты компании</CardTitle><CardDescription>Используются при генерации документов.</CardDescription></CardHeader><CardContent className="grid gap-3 md:grid-cols-2">{(Object.keys(settings.company) as Array<keyof CrmSettings['company']>).map((field) => <Input key={field} value={settings.company[field]} onChange={(event) => updateCompanyField(field, event.target.value)} placeholder={field} />)}</CardContent></Card><Card><CardHeader><CardTitle>Шаблоны документов</CardTitle><CardDescription>Тексты поддерживают переменные dealTitle, clientName, revenue, dealId, companyLegalName.</CardDescription></CardHeader><CardContent className="space-y-3">{settings.documentTemplates.map((template) => <div key={template.id} className="space-y-2 rounded-2xl border border-slate-200 p-3"><div className="grid gap-2 md:grid-cols-2"><Input value={template.title} onChange={(event) => updateDocumentTemplateConfig(template.id, { title: event.target.value })} /><Input value={template.filePrefix} onChange={(event) => updateDocumentTemplateConfig(template.id, { filePrefix: event.target.value })} /></div><textarea value={template.body} onChange={(event) => updateDocumentTemplateConfig(template.id, { body: event.target.value })} className="min-h-24 w-full rounded-md border border-slate-200 px-3 py-2 text-sm" /><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={template.isActive} onChange={(event) => updateDocumentTemplateConfig(template.id, { isActive: event.target.checked })} /> Активен</label></div>)}</CardContent></Card></div></section>;
}
